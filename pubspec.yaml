name: gestion_wifi
description: Gestion hors-ligne des ventes et tickets Wi-Fi.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  sqflite: ^2.4.2+1
  path: ^1.9.1
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  permission_handler: ^12.0.1
  share_plus: ^11.0.0
  path_provider: ^2.1.5
  file_picker: ^8.1.2
  image_picker: ^1.1.2
  google_mlkit_text_recognition: ^0.15.0
  print_bluetooth_thermal: ^1.1.6
  esc_pos_utils_plus: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
